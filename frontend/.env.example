NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Old CRA names are no longer read by browser code in Next.js.
# If you still have REACT_APP_API_URL in frontend/.env, copy the same value to NEXT_PUBLIC_API_URL.
